import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({

    container: {
        flex: 1,
        backgroundColor: '#fff',
        marginVertical: 10,
        marginHorizontal: 10
    },
    borderStyle: {
        borderWidth: 1,
        borderColor: '#ccc',
    }

})